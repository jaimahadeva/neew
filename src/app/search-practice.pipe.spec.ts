import { SearchPracticePipe } from './search-practice.pipe';

describe('SearchPracticePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPracticePipe();
    expect(pipe).toBeTruthy();
  });
});
